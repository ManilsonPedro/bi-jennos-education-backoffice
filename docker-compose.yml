services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      # Aponta para a API backend (repositorio bi-jennos-education-api).
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:8000/api/v1}
    restart: unless-stopped
